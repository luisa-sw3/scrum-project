{% extends 'BackendBundle:Default:mainFancybox.html.twig' %}
{% block title %}{{'backend.user_project.add_collaborator'|trans}}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                {{'backend.user_project.add_collaborator'|trans}}
            </h1>
        </div>
    </div>

    <div class="row">
        {{ form_start(form, {'action': path('backend_project_team_add',{'id':project.id}), 'method': 'POST'}) }}
            <div class="col-lg-6">
                {{ form_widget(form.userId) }}
                <div class="form-group">
                    {{ form_label(form.email)}}
                    {{ form_widget(form.email, {'attr':{'class':'form-control'}}) }}
                    {{ form_errors(form.email) }}
                </div>
            </div>
            
        
            <div class="col-lg-12">
                {{ form_widget(form._token) }} 
                <button type="submit"class="btn btn-primary pull-right">{{ 'backend.user_project.create'|trans }}</button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
{% block scripts %}
    singleHtmlEditor('textarea', 200);
{% endblock %}
